package dio.me.collections.primeiro

fun main(){

    /*Aula 1

    //Criação e utilização do Array de Int
    val values = IntArray(5)

    values[0]=1
    values[1]=2
    values[2]=3
    values[3]=4
    values[4]=5

    println("primeira forma de iterar")
    for (valor in values){
        println(valor)
    }

    println("segunda forma de iterar")
    values.forEach{valor ->
        println(valor)
    }

    println("terceira forma de iterar")
    for(index in values.indices){
        println(values[index])
    }

    Aula 1 */


    /*Aula 2

    sem a necessidade de falar o valor
    val values = intArrayOf(9,18,2,2,1,100,0)

    values.forEach { println(it) }

    println("Ordenado")
    values.sort()
    values.forEach { println(it) }

     Aula 2*/

    /*Aula 3
    val nomes = Array(3){""}
    nomes[0]="Maria"
    nomes[1]="João"
    nomes[2]="José"

    for (nome in nomes){
        println(nome)
    }

    nomes.sort()
    println("Ordenado")
    nomes.forEach { nome ->
        println(nome)
    }
    val nomes2 = arrayOf("Zazá","Pedro","Arnaldo")
    nomes2.sort()
    nomes2.forEach { println(it) }

    Aula 3*/

    /*Aula 4

    val salarios = DoubleArray(3)
    salarios[0]=1000.0
    salarios[1]=3000.0
    salarios[2]=500.0

    salarios.forEach { println(it) }
    salarios.forEachIndexed{index,salario ->
        salarios[index] = salario*1.10
    }

    println("Aumento de salário")
    salarios.forEach { println(it) }

    val salarios2 = doubleArrayOf(110.0,230.0,9.8)

    salarios2.forEach { println(it) }*/

    

}